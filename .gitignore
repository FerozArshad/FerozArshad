# ─────────────────────────────────────────────────────────────────────────────
#  Feroz Arshad — main site
#  Anything that has secrets, local-only state, IDE chrome, or large generated
#  data must be blocked here. If you're unsure: gitignore it.
# ─────────────────────────────────────────────────────────────────────────────

# ── Secrets / env ──
.env
.env.*
!.env.example
.n8n-secrets.txt
*.pem
*.key
*.p12
*-secrets.*
credentials.json
.mcp-credentials/
oauth-client*.json
client_secret*.json

# ── Dependencies ──
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ── Build output ──
/.next/
/out/
/build
/dist
/coverage
*.tsbuildinfo
next-env.d.ts

# ── Generated Prisma client ──
/src/generated/prisma
/prisma/migrations/dev.db
*.sqlite
*.sqlite-journal

# ── Editor / IDE chrome ──
.DS_Store
Thumbs.db
.cursor/
.cursorignore
.idea/
.vscode/
*.swp
*~

# ── Vercel ──
.vercel

# ── Debug logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── LOCAL-ONLY: scrapers, data dumps, working notes ──
# These exist on this machine for the operator's personal workflow.
# They are NOT part of the published site and must NEVER be committed.
dashboard/
.design-import/
.claude/
fiverr-conversation-extractor-main/
fiverr-exporter/
fiverr.html
upwork.html
*_autopilot_extract_*.json
n8n_*.json
n8n_*.bat
n8n_*.sh
ai.md
ai/
tasks/
check-leads.js
maintenance.js
copy-static.js
.htaccess

# ── OS / archives ──
*.log
*.tar
*.tar.gz
*.zip
*.7z

.vercel
.env*
